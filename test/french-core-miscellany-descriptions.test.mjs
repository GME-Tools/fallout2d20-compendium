import test from"node:test";import assert from"node:assert/strict";import{readFile,readdir}from"node:fs/promises";import path from"node:path";
test("French miscellany descriptions are all translated",async()=>{const en=new Map();for(const f of await readdir("generated/source-packs/en/miscellany.db")){if(f.endsWith(".json")){const d=JSON.parse(await readFile(path.join("generated/source-packs/en/miscellany.db",f),"utf8"));en.set(d._id,d)}}for(const f of await readdir("generated/source-packs/fr/miscellany.db")){if(!f.endsWith(".json"))continue;const fr=JSON.parse(await readFile(path.join("generated/source-packs/fr/miscellany.db",f),"utf8")),source=en.get(fr._id);if(source.system?.description){assert.ok(fr.system.description,source.name);assert.notEqual(fr.system.description,source.system.description,source.name)}}});
