# Historical evidence

This directory preserves completed stories, superseded inventories, audit trails and
decision snapshots. It is intentionally excluded from normal agent context.

Use it only when a task requires historical evidence, regression archaeology or
verification of a past owner decision. Current execution contracts live in `docs/`
and the active story is named in `docs/STATE.md`.
